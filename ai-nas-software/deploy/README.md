# deploy/ — 部署

与设备部署、烧录、升级相关的文件。

| 子目录 | 内容 |
|--------|------|
| `scripts/` | 烧录 / 部署 / 升级脚本（如镜像写入、分区、首次启动配置） |
| `configs/` | 配置模板（网络、存储、服务启停等，不含任何真实密钥） |

> **严禁在此提交 `.env`、私钥、密码或任何凭证。** 配置模板中的敏感字段一律以占位符（如 `<API_KEY>`）表示，真实值通过部署时的密钥管理注入。
