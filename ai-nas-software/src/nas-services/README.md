# src/nas-services/ — 自研 NAS 服务

AI NAS 的核心自研服务，可包含（按需分模块）：

- 存储编排与管理（卷、配额、快照、RAID 之上的增值能力）；
- 网络与共享协议适配（SMB/NFS/iSCSI 等之上的增强）；
- AI 推理服务（本地模型加载、推理调度）；
- 监控、告警与运维 Agent。

以 **Mulan PSL v2** 许可发布。若某模块为 OMV 插件且构成衍生作品，须保持 GPLv3 兼容并在文件头声明。
